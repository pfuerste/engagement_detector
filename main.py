import os
from io import encodings_identity as ei
from io import io_utils
from io import face_extract as fe
from io.utils import crop_bbs


def main():
    pass
    # read config (developer info)
    # load model
    # open gui

    # read from gui:
    # lecture name (new or dropdown of old ones)
    # input method (default: screenshot)
    # inference intervals (?)

    # loop while (not stop button pressed)
    # get image
    # find faces
    # get encodings
    # compare to last encodings
    # inference
    # match encodings & metrics
    # calc intra-session metrics
    # update gui

    # save data
